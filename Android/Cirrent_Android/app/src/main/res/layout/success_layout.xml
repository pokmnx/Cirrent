<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffffff"
    >

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:src="@drawable/xfinity"
        android:layout_centerHorizontal="true"
        android:id="@+id/providerimage"
        android:scaleType="fitCenter"
        android:padding="16dp"
        />

    <TextView
        android:text="Your Device is connected"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:id="@+id/connected_label"
        android:textColor="@android:color/background_dark"
        android:layout_below="@+id/providerimage"
        android:textSize="18sp" />

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        app:srcCompat="@drawable/checkmark"
        android:layout_below="@+id/connected_label"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="36dp"
        android:id="@+id/checkmark" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textAlignment="center"
        android:autoLink="web"
        android:text="www.cirrent.com"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:id="@+id/learn_more_url"
        />

</RelativeLayout>